#include <Windows.h>
#include <iostream>
#include <string>
using namespace std;
int main()
{
	SetConsoleOutputCP(1251);
	/*cout << "Перевидіть число з 10 сч в 2 сч: ";
	int a;
	cin >> a;
	string b = "";
	for (int i = a; 1 <= i; i = i / 2) {
		b = char(i % 2 + '0') + b;
	}
	cout << b;*/

	/*cout << "Перевидіть число з 10 сч в 8 сч: ";
	int a;
	cin >> a;
	string b = "";
	for (int i = a; 1 <= i; i = i / 8) {
		b = char(i % 8 + '0') + b;
	}
	cout << b;*/

	








	/*cout << "Перевидіть число з 10 сч в 16 сч: ";
	int a;
	cin >> a;
	string b = "";
	for (int i = a; i >= 1; i = i / 16) {
		b = char(i % 16 + '0') + b;
	}
	cout << b;*/
	
























}
